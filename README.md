balgo is a library of some BAsic ALGOrithms.

## Implemented Algorithms
* Double array trie
* Ternary trie
* Aho–Corasick automata
 
## Build
balgo uses cmake as the build tool.

```
mkdir -p build
cd build
cmake -DCMAKE_INSTALL_PREFIX=<install-prefix> ..
make
make install
```
